<h2> Shorten your link </h2>
  <%= form_for @url, url: {controller: "urls", action: "create"} do |f| %>

  <%= f.label :url %>
  <%= f.text_field :main_url %> <br>
  <%= f.submit 'CREATE URL',class: 'btn btn-xl' %>
  <% end %>